web:
  image: nginx:latest
  ports:
    - "8081:80"
  volumes:
    - ./www:/www
    - ./config/nginx.conf:/etc/nginx/conf.d/site.conf
  links:
    - php

php:
  image: php:7-fpm
  volumes:
      - ./www:/www